.claude
INSTRUCTIONS.md
results/**
**/vendor/**

# Prebuilt server binaries: frankenphp alone is 163M, over GitHub's 100M per-file limit.
franken/frankenphp
roadrunner/rr
swoole/swoole.so
rapira/rapira
rapira/rapira-*

# nginx configs rendered by start_fpm for a non-default port (fleet mode); the
# checked-in nginx.conf / nginx.scenario.conf are the sources and stay tracked.
fpm/nginx.*.generated.conf

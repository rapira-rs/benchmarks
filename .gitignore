.claude
INSTRUCTIONS.md
results/
**/vendor/**

# Terraform working files; the tfstate and the key carry secrets, the tfvars
# carries the operator IP.
terraform/.terraform/
terraform/*.tfstate*
terraform/*.tfvars
terraform/rig-key.pem

# ssh session files the drivers create.
.ssh-known-hosts
.ssh-cm-*
